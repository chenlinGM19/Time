<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 开机自启动权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarClock"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.CarClock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".FloatingClockService" 
                 android:enabled="true"
                 android:exported="false" />

        <!-- 静态注册广播接收器，包含开机广播监听 -->
        <receiver android:name=".CommandReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="com.example.carclock.ACTION_TOGGLE_VISIBILITY" />
                <action android:name="com.example.carclock.ACTION_TOGGLE_PASSTHROUGH" />
                <action android:name="com.example.carclock.ACTION_INCREASE_SIZE" />
                <action android:name="com.example.carclock.ACTION_DECREASE_SIZE" />
                <action android:name="com.example.carclock.ACTION_CHANGE_STYLE" />
                <action android:name="com.example.carclock.ACTION_TOGGLE_SECONDS" />
                <action android:name="com.example.carclock.ACTION_TOGGLE_BG" />
                <action android:name="com.example.carclock.ACTION_TOGGLE_WEIGHT" />
                <action android:name="com.example.carclock.ACTION_RESET_POSITION" />
                <action android:name="com.example.carclock.ACTION_TOGGLE_ORIENTATION" />
                <action android:name="com.example.carclock.ACTION_TOGGLE_TOASTS" />
                <action android:name="com.example.carclock.ACTION_SET_VISIBLE" />
                <action android:name="com.example.carclock.ACTION_SET_BLOCKING" />
                <action android:name="com.example.carclock.ACTION_SET_OPACITY" />
            </intent-filter>
        </receiver>

    </application>

</manifest>